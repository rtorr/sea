[package]
name = "cjson"
version = "1.7.0"
kind = "source"
description = "Ultralightweight JSON parser in ANSI C"
license = "MIT"

[build]
script = "build.sh"

[publish]
registry = "test-local"
include = ["include/**", "lib/**"]
